# Absolute Minimal Dependencies for MySecondMind Bot (Under 300MB)
# Core FastAPI only
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Essential HTTP and Telegram
httpx>=0.27.0,<0.28.0
python-telegram-bot>=21.0.0
requests>=2.31.0
jinja2>=3.1.2
python-multipart>=0.0.9

# Core functionality only
groq>=0.15.0
python-dotenv>=1.0.0
supabase>=2.8.0
cryptography>=43.0.0
beautifulsoup4>=4.12.0

# Essential time parsing only
parsedatetime>=2.6
python-dateutil>=2.8.2
pytz>=2023.3

# Lightweight scheduling (only ~5MB)
APScheduler>=3.10.4

# Keep these removed (too heavy):
# numpy        # Remove ML features (~30MB)
# nltk         # Remove text processing (~50MB)  
# faiss-cpu    # Remove vector search (~50MB)
# sentence-transformers  # Too heavy (~200MB)
# torch        # Way too heavy (~200MB)