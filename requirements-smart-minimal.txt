# Smart Minimal Dependencies - Enhanced AI with Memory Safety (~320MB)
# Phase 1: Add essential ML without breaking 512MB limit

# Core FastAPI and web
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# Essential HTTP and Telegram  
httpx>=0.27.0,<0.28.0
python-telegram-bot>=21.0.0
requests>=2.31.0

# Core functionality
groq>=0.15.0
python-dotenv>=1.0.0
supabase>=2.8.0
cryptography>=43.0.0
beautifulsoup4>=4.12.0

# Time parsing
parsedatetime>=2.6
python-dateutil>=2.8.2
pytz>=2023.3

# Scheduling
APScheduler>=3.10.4

# 🧠 SMART ENHANCEMENTS (Phase 1):
numpy>=1.24.0                    # ~30MB - Essential for ML
scikit-learn>=1.3.0             # ~40MB - TF-IDF, clustering, similarity
psutil>=5.9.0                   # ~5MB  - Memory monitoring

# Phase 2 candidates (test Phase 1 first):
# sentence-transformers>=2.2.0  # ~80MB - Real semantic embeddings
# chromadb>=0.4.0               # ~30MB - Vector database
# pandas>=2.0.0                 # ~50MB - Data analysis

# Still avoiding heavy packages:
# torch         # ~200MB - Too heavy
# transformers  # ~150MB - Too heavy  
# faiss-cpu     # ~50MB  - Can add in Phase 2
# nltk          # ~50MB  - Can add in Phase 2
